{% extends 'AWDFootballBundle::layout.html.twig' %}

{% block content%}
    <h1>Team</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>


        <a href="{{ path('awd_team') }}" class="btn btn-primary"> Back to the list</a>

        <a href="{{ path('awd_team_edit', { 'id': entity.id }) }}" class="btn btn-success"> Edit </a>

{{ form(delete_form) }}
{% endblock %}
