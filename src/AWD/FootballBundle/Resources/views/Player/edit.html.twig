{% extends 'AWDFootballBundle::layout.html.twig' %}
{% block title %}Player edit{% endblock %}
{% block nav %}
    <ul class="nav navbar-nav">
            <li ><a href="{{ path('awd_football_homepage') }}">Home</a></li>
            <li><a href="{{ path('awd_team') }}">Teams</a></li>
            <li class="active"><a href="{{ path('awd_player') }}">Players</a></li>
            <li><a href="{{ path('awd_game') }}">Games</a></li>
            
          </ul>
{% endblock %}
{% block content%}
    <h1>Player edit</h1>
<div class="row">
    <div class="col-lg-8">
        {{ form_start(edit_form) }}
            {{ form_errors(edit_form) }}
        {{ form_end(edit_form) }}{{ form(delete_form) }}
    </div>

    <div class="col-lg-4"><a href="{{ path('awd_player') }}" class="btn btn-primary">
            Back to the list
        </a>
    </div>
</div>   
{% endblock %}
